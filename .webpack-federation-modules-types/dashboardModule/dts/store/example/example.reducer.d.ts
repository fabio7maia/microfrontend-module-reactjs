import { ExampleState } from './example.types';
import { AnyAction } from 'redux';
export declare const exampleInitialState: ExampleState;
export declare const exampleReducer: (state: ExampleState | undefined, action: AnyAction) => ExampleState;
